#ifndef __LED_CTRL_H__
#define __LED_CTRL_H__
#include "main.h"

/*============================================================================
 *                     旧版LED控制 (兼容保留)
 *===========================================================================*/
void LED_FLAG_LOOP(void);
void LED_FLAG_Run(void);

/*============================================================================
 *                     新版LED指示器组件
 *===========================================================================*/

/**
 * @brief 初始化LED指示器组件
 * @note 在系统初始化时调用一次，需要在 system_tick_ms 可用之后调用
 */
void LedIndicator_BoardInit(void);

#endif
